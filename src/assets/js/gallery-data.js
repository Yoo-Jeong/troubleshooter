/* ============================================================
   갤러리 목록 (단일 출처)
   gallery/index.html(실제 목록 화면)과 홈 화면(노드의 "N RECORDS" 표시)이
   이 파일 하나를 같이 봐요 — 항목을 추가/삭제하면 두 화면에 자동 반영됩니다.

   두 가지 종류를 올릴 수 있어요:
   · 그림  → type 없이(또는 type:'art') 채우면 됨. 클릭하면 그 자리에서 확대(라이트박스)로 보여줌.
   · 글(연성 등) → type:'fic' 로 하고 url 에 외부 주소(AO3·블로그·트위터 등)를 적음.
     클릭하면 그림과 똑같이 먼저 라이트박스로 표지+정보를 보여주고, 그 안의 "원문 보러가기" 버튼을
     눌러야 그때 그 주소가 새 탭으로 열림(바로 이동 안 함). 카드에 작은 "TEXT" 표시가 자동으로 붙음.
     (글도 목록에 보일 표지 그림은 필요 — file 은 그림/글 둘 다 필수)

   나중에 항목을 추가할 때:
   1) 표지로 쓸 이미지 파일을 assets/img/gallery/ 폴더에 넣는다
   2) 아래 목록에 { } 항목을 하나 복사해서 붙여넣고 값만 바꾼다
   날짜가 최신인 항목이 갤러리 화면에서 자동으로 맨 위에 옴 — 순서를 직접 맞출 필요 없음.
   ※ 원본 이미지 용량이 크면(수 MB 이상) 뜨는 데 시간이 걸릴 수 있어요.
      웹에 올릴 땐 가로 폭 2000px 안팎으로 줄여서 넣는 걸 권장.
   ============================================================ */
window.GALLERY_ITEMS = [
  {
    file:  '2025-chuseok.png',     // assets/img/gallery/ 안의 표지 이미지 파일 이름
    title: '2025 추석이메',         // 제목
    date:  '2025-10-06',           // 날짜 (YYYY-MM-DD) — 정렬 기준
    artist: '',                    // 그린 사람 (예: '이름' · 없으면 빈 칸)
    characters: ['벡스터', '민트', '메릴리', '마이티', '에스', '셀루카'],   // 등장 캐릭터 (없으면 빈 칸)
    desc:  ''                      // 한 줄 설명 (없으면 빈 칸)
    // type: 'fic', url: 'https://...'  ← 그림이 아니라 글이면 이 두 줄을 추가(주석 해제하고 값 채우기)
  }
];
